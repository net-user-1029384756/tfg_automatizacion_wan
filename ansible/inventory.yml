all:
  children:
    routers:
      vars:
        ansible_connection:   network_cli
        ansible_network_os:   routeros
        ansible_user:         admin
        ansible_become:       false
      hosts:
        R1:
          ansible_host:     192.168.233.130
          ansible_password: "{{ lookup('env', 'R1_PASS') }}"
        R2:
          ansible_host:     192.168.233.131
          ansible_password: "{{ lookup('env', 'R2_PASS') }}"
        R3:
          ansible_host:     192.168.233.132
          ansible_password: "{{ lookup('env', 'R3_PASS') }}"
        R4:
          ansible_host:     192.168.233.133
          ansible_password: "{{ lookup('env', 'R4_PASS') }}"
